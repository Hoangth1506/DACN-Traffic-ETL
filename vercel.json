{
  "buildCommand": "cd dashboard && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "dashboard/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets/|.*\\.json$|.*\\.ico$).*)",
      "destination": "/index.html"
    }
  ]
}
