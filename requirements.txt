# Core data processing
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0

# API & HTTP
requests>=2.31.0
httpx>=0.27.0          # Modern async HTTP client for TomTom API
tenacity>=8.2.3        # Smart retry logic with exponential backoff

# Configuration
pyyaml>=6.0.1

# Spatial & Scientific
scipy>=1.13.0
scikit-learn>=1.4.0

# Caching & Performance
diskcache>=5.6.3       # Persistent caching for OSM/KDTree data
joblib>=1.3.2          # Parallel processing improvements

# Data storage & archival
duckdb>=0.10.0         # Fast analytical database for archival storage

# Logging & Monitoring
tqdm>=4.66.0
structlog>=24.1.0      # Structured logging for better observability

# Document generation (existing)
python-docx>=1.1.0
reportlab>=4.0.0
